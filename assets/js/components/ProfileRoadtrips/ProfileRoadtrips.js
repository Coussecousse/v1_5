import React from "react";
import profilStyles from '../Profile/Profile.module.css';

export default function ProfileRoadtrips() {
    return (
        <section className={`first-section ${profilStyles.section}`}>
            <h1 className={`typical-title ${styles.title}`}>Profil - Roadtrips</h1>
            
        </section>
    )
}