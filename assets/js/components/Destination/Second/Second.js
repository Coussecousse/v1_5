import React from "react";
import styles from './Second.module.css';

export default function Second() {
    return (
        <section className={`${styles.section} typical-section`}>
            <h2 className={styles.title}>Découvrez d'autres roadtrips</h2>
        </section>
    )
}